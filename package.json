{
  "name": "zharif-portfolio",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "predev": "npm run compile:images",
    "dev": "vite",
    "compile:images": "node scripts/compile-images.mjs",
    "prebuild": "npm run compile:images",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "gsap": "latest",
    "marked": "^18.0.3",
    "react": "latest",
    "react-dom": "latest",
    "vite": "latest"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.37.1",
    "sharp": "^0.34.5",
    "wrangler": "^4.92.0"
  }
}
